apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stok-operator.fullname" . }}
  labels:
    {{- include "stok-operator.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "stok-operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "stok-operator.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "stok-operator.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}{{- include "stok-operator.tag" . -}}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
          - stok-operator
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "stok-operator"
