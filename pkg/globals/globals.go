package globals

const (
	RunnerContainerName = "runner"
	// TerraformPath is the absolue path to the directory on the runner
	// container containing the terraform binary
	TerraformPath = "/terraform-bins"
)
