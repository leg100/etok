apiVersion: apps/v1
kind: Deployment
metadata:
  name: terraform-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: terraform-operator
  template:
    metadata:
      labels:
        name: terraform-operator
    spec:
      serviceAccountName: terraform-operator
      containers:
        - name: terraform-operator
          # Replace this with the built image name
          image: terraform-operator:latest
          command:
          - terraform-operator
          imagePullPolicy: Never
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "terraform-operator"
